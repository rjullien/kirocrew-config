# kirocrew-config

Custom image for [Kiro Crew](https://kiro.dev/crew/) on the personal k3s cluster (same pattern as `hermes-leo-config`).

Base: `ghcr.io/kirodotdev/kirocrew` (pinned). Runtime GitOps lives in `BaptTF/vps-infra`.

No secrets in this repo.
